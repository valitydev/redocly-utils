# See https://redocly.com/docs/cli/configuration/ for more information.
extends:
  - recommended

plugins:
  - "./plugins/merge-schemas.js"

rules:
  tag-description: off
  no-empty-servers: off

preprocessors:
  local/merge-schemas: on
